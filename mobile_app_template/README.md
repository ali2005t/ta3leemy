# 📱 قالب تطبيق المعلم (Flutter Mobile App)

هذا هو الكود المصدري (Source Code) لإنشاء تطبيق موبايل (أندرويد & أيفون) لأي معلم على منصة **Ta3leemy**.

## 🚀 المميزات
1.  **WebView متطور:** يدعم رفع الملفات، والكاميرا، وتخزين كلمات المرور.
2.  **حماية المحتوى (Anti-Screenshot):**
    *   **Android:** يمنع التقاط الشاشة تماماً (شاشة سوداء).
    *   **iOS:** يخفي المحتوى عند فتح قائمة التطبيقات المفتوحة (Background).
3.  **سهل التعديل:** ملف واحد فقط `config.dart` لتغيير كل حاجة.

---

## 🛠️ طريقة التعديل (لكل مدرس)

1.  افتح الملف: `lib/config.dart`
2.  عدل البيانات التالية:
    ```dart
    static const String appName = "اسم المدرس هنا";
    static const String startUrl = "رابط موقع المدرس هنا";
    static const Color primaryColor = Color(0xFF6366f1); // كود لون المدرس
    ```
3.  احفظ الملف.

---

## 🏗️ طريقة الاستخراج (Build)
يجب أن يكون لديك **Flutter SDK** مثبت على جهازك.

### 1️⃣ Android (APK)
لإنشاء ملف APK للأندرويد، افتح التيرمنال واكتب:
```bash
flutter build apk --release
```
ستجد الملف في: `build/app/outputs/flutter-apk/app-release.apk`

### 2️⃣ iOS (IPA)
يحتاج جهاز Mac. افتح المشروع ببرنامج Xcode وقم بعمل Archive.
```bash
flutter build ios
```

---

## 📂 هيكل الملفات الهامة
*   `lib/config.dart`: ⚙️ ملف الإعدادات (عدل هنا).
*   `lib/splash_screen.dart`: 🎨 شاشة البداية (الشعار).
*   `lib/webview_screen.dart`: 🌐 كود المتصفح.
*   `android/app/src/main/.../MainActivity.kt`: 🔒 كود حماية الأندرويد.
*   `ios/Runner/AppDelegate.swift`: 🔒 كود حماية الأيفون.
*   `assets/images/`: 🖼️ ضع صورة اللوجو هنا باسم `logo.png`.

---

## ❓ أسئلة شائعة (FAQ)

### 1- أين أضع صورة اللوجو؟
قم بوضع صورة اللوجو في المجلد: `mobile_app_template/assets/images/`
**يجب** أن يكون اسم الصورة `logo.png` (أو عدل الاسم في `config.dart`).
حجم الصورة المفضل: 512x512 بكسل بخلفية شفافة (PNG).

### 2- كيف أغير رقم الإصدار (Version)؟
في ملف `pubspec.yaml`، ابحث عن السطر:
```yaml
version: 1.0.0+1
```
*   `1.0.0`: هو الاسم الظاهر للمستخدم (مثل 2.5.0).
*   `+1`: هو رقم البناء (Build Number) للمتجر (يجب زيادته في كل تحديث +1).

### 3- ماذا لو غير المعلم اللوجو أو الاسم لاحقاً؟
بما أن هذا "تطبيق واجهات" (Native App)، فإن بيانات شاشة البداية (اللوجو والاسم) مدمجة داخل التطبيق.
*   **لو المعلم غير اللوجو على الموقع:** التطبيق مش هيحس بالتغيير في شاشة البداية، لكن محتوى الموقع جوه التطبيق هيتحدث عادي.
*   **الحل:** لو عايز تغير اللوجو في شاشة البداية، لازم تغير الصورة في الكود وتبني نسخة جديدة (Update) وترفعها المتجر.

### 4- كيف أجعل اسم المبرمج (Developed by) يظهر؟
شاشة البداية `splash_screen.dart` مصممة لتعرض:
1.  لوجو المعلم.
2.  اسم التطبيق.
3.  في الأسفل: **Developed by Ta3leemy**.

استمتع بإنشاء تطبيقاتك! 🚀
