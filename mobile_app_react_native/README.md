# 📱 قالب تطبيق المعلم (React Native / Expo)

هذا الإصدار مبني باستخدام **Expo (React Native)**. يتميز بأنه أسهل في التسطيب (لأنه يستخدم JavaScript) ويعمل بكفاءة عالية.

## 🚀 الفرق عن Flutter
*   **اللغة:** يستخدم JavaScript (نفس لغة الموقع) بدلاً من Dart.
*   **الحماية:** يستخدم مكتبة `expo-screen-capture` لمنع التصوير بسهولة.
*   **السهولة:** لا يحتاج لتسطيب Android Studio بالكامل في البداية لتجربته.

---

## 🛠️ طريقة التشغيل (المتطلبات)
تحتاج فقط لبرنامج **Node.js** مثبت على جهازك.

1.  افتح التيرمنال في هذا المجلد:
    ```bash
    cd mobile_app_react_native
    ```

2.  حمل المكتبات (مرة واحدة):
    ```bash
    npm install
    ```

3.  شغل التطبيق:
    ```bash
    npx expo start
    ```

4.  سيظهر لك **QR Code**. امسحه بكاميرا الموبايل (تحتاج تطبيق **Expo Go** من المتجر) وسيعمل التطبيق فوراً على هاتفك للتجربة!

---

## ⚙️ طريقة التعديل
*   **`config.js`**: ملف واحد فيه اسم التطبيق، الرابط، واللون. عدله براحتك.

## 🏗️ استخراج التطبيق (APK)
في Expo، يتم بناء التطبيق سحابياً (Cloud Build) بسهولة جداً:
1.  أنشئ حساب على [expo.dev](https://expo.dev).
2.  اكتب الأمر:
    ```bash
    npx eas build -p android --profile preview
    ```
وسيقوم السيرفر ببناء ملف APK لك وتحميله.
